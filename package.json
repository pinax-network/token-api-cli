{
  "name": "@pinax/token-api-cli",
  "version": "0.1.4",
  "description": "Pinax Token API CLI - Power your apps & AI agents with real-time token data",
  "type": "module",
  "bin": {
    "token-api": "./dist/index.mjs"
  },
  "files": [
    "dist",
    "src",
    "bin"
  ],
  "scripts": {
    "prepublishOnly": "bun run build",
    "build": "bun build ./cli.ts --outfile ./dist/cli.mjs --target=node",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write ."
  },
  "keywords": [
    "pinax",
    "token-api",
    "blockchain",
    "evm",
    "ethereum",
    "sdk"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pinax-network/token-api-cli.git"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5",
    "@biomejs/biome": "^2.3.8"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "dotenv": "^17.2.3",
    "@pinax/token-api": "^0.1.3"
  }
}